<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:TunisiaPrayer.ViewModels"
             x:Class="TunisiaPrayer.Views.ChapterPage"
             BackgroundColor="Black">

    <ContentPage.BindingContext>
        <viewmodels:ChapterViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>
            <ListView ItemsSource="{Binding Verses.verses}"
                      CachingStrategy="RecycleElement"
                      HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid ColumnDefinitions="5*,*,5*" Padding="10">
                                <Label Text="{Binding text_uthmani}" Grid.Column="0" HorizontalTextAlignment="Center" TextColor="White"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>